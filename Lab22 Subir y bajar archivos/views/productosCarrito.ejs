<%- include('includes/head.ejs') %>
        <main>
            <div class="container">
                <% if(productosNuevos.length > 0 && isLoggedIn !== undefined){%>
                    <h2>Los productos que quieres ver en nuestra tienda próximamente</h2>
                    <ul>
                    <% for(productoNuevo of productosNuevos) { %>
                        <li> <%= productoNuevo.nombre %> </li>
                        <img src="/<%= productoNuevo.imagen %>">
                    <% } %>    
                    </ul>
                <%} %>
            </div>
            <div class="container">
                <h1 class="cartitle">Carrito de compras</h1>
                <p>Compraste:</p>
                <ul>
                    <% 
                        for (let producto of productos){ 
                            if(producto.en_carrito >= 1){
                    %>
                                <li> <%= producto.en_carrito %>- <%=  producto.nombre%> </li>
                    <% 
                            } 
                        }
                    %>
                </ul>
                <br>
                <h2>Subtotal: $<span id="subt">0</span></h2>
                <h3>Iva: $<span id="iva">0</span></h3>
                <h1>Total: $<span id="total">0</span></h1>
            </div>     
        </main>
<%- include('includes/footer.ejs') %>